This is a code
or cloning the folder use command git clone link
To come out of folder use c ..
To select any folder use command cd foldername 
To commit use git commit -m "add new files"
To push project use git origin main